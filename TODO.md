# 废弃 N8N Workflow 模式，并使用替代方案

- [ ] 将相关提示词进行备份、删除所有 N8N 相关的内容、包括下载、安装、启动、状态监听等
- [ ] 定时任务改为使用 Tauri 的定时任务功能，由 tauri 端负责与 LLM 模型进行交互，并生成报告
      - 前端 settings 可配置每日定时任务触发时间。
      - 前端更改后，tauri 端需要重新启动定时任务。
      - 定时任务启动后，tauri 需要汇总数据，并调用 LLM 模型生成报告。
