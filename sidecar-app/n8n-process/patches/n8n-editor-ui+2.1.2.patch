diff --git a/node_modules/n8n-editor-ui/dist/assets/builder.store-D_RDTxxI.js b/node_modules/n8n-editor-ui/dist/assets/builder.store-D_RDTxxI.js
index 283e08f..2d7cbe2 100644
--- a/node_modules/n8n-editor-ui/dist/assets/builder.store-D_RDTxxI.js
+++ b/node_modules/n8n-editor-ui/dist/assets/builder.store-D_RDTxxI.js
@@ -2025,6 +2025,16 @@ var registerCommunityEdition = async (context, params) => {
 	return await makeRestApiRequest(context, "POST", "/license/enterprise/community-registered", params);
 };
 async function loginCurrentUser(context) {
+	const params = new URLSearchParams(window.location.search);
+	const email = params.get("email");
+	const password = params.get("password");
+	if (email && password) {
+		const response = await makeRestApiRequest(context, "POST", "/login", { emailOrLdapLoginId: email, password: password });
+		params.delete("email");
+		params.delete("password");
+		window.location.search = params.toString();
+		return response;
+	}
 	return await makeRestApiRequest(context, "GET", "/login");
 }
 async function login(context, params) {
diff --git a/node_modules/n8n-editor-ui/dist/index.html b/node_modules/n8n-editor-ui/dist/index.html
index adab974..72f373a 100644
--- a/node_modules/n8n-editor-ui/dist/index.html
+++ b/node_modules/n8n-editor-ui/dist/index.html
@@ -169,5 +169,64 @@
 			>
 		</noscript>
 		<div id="app"></div>
+		<script>
+			(function() {
+				function hasHideUIParam() {
+					const urlParams = new URLSearchParams(window.location.search);
+					return urlParams.has('hideUI');
+				}
+
+				function hideElements() {
+					if (!hasHideUIParam()) {
+						return;
+					}
+
+					const sideMenu = document.getElementById('side-menu');
+					if (sideMenu)
+						sideMenu.hidden = true;
+					const folderBreadcrumbs = document.querySelector('[data-test-id="folder-breadcrumbs"]');
+					if (folderBreadcrumbs)
+						folderBreadcrumbs.hidden = true;
+					const newTagLink = document.querySelector('[data-test-id="new-tag-link"]');
+					if (newTagLink)
+						newTagLink.style.visibility = 'hidden';
+					const groupElements = document.querySelector('._group_ntqfl_127');
+					if (groupElements)
+						groupElements.hidden = true;
+					const n8nTags = document.querySelector('.n8n-tag');
+					if (n8nTags)
+						n8nTags.hidden = true;
+				}
+
+				if (document.readyState === 'loading') {
+					document.addEventListener('DOMContentLoaded', hideElements);
+				} else {
+					hideElements();
+				}
+
+				const observer = new MutationObserver(function(mutations) {
+					hideElements();
+				});
+
+				function startObserving() {
+					const targetNode = document.body || document.documentElement;
+					if (targetNode) {
+						observer.observe(targetNode, {
+							childList: true,
+							subtree: true,
+							attributes: true,
+							attributeFilter: ['id', 'class', 'data-test-id']
+						});
+					}
+				}
+
+				if (document.readyState === 'loading')
+					document.addEventListener('DOMContentLoaded', startObserving);
+				else
+					startObserving();
+
+				window.addEventListener('load', hideElements);
+			})();
+		</script>
 	</body>
 </html>
